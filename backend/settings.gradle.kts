plugins {
    // Downloads JDK 21 for the Gradle toolchain when none is installed locally
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "support-desk-backend"
