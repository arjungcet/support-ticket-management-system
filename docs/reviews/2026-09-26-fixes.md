# Defect Fixes after the Acceptance Review — 2026-09-26

Scope: only **confirmed defects in existing code** from `2026-09-26-acceptance-review.md` §4 (items 3 and 5). Each
fix started with a regression test that was run and seen failing for the reported reason.

| Fix | Defect | Affects | Change | Regression test | Before → after |
|-----|--------|---------|--------|-----------------|----------------|
| F-1 | **M-1:** the edit form sent untouched stale fields after a reload, reverting other users' changes (lost update) | AC-4, REQ-4, `api-contract.md` §1.2, `state-machine.md` §9 | `EditTicketForm.tsx`: send only `dirtyFields`, and re-base untouched fields on the reloaded ticket (`reset(…, { keepDirtyValues: true })`) | `TicketDetailsView.test.tsx`: "…saving sends only the user's changes (no lost update, M-1)" | ❌ body had `"description": "Original description"` → ✅ `{ version: 3, title: "My title" }` |
| F-2 | **M-2:** the search box re-applied an old keyword after Back or nav-link navigation | AC-7, REQ-6, `architecture.md` §6.2 | `TicketListView.tsx`: follow URL changes the component didn't push itself. Its own (possibly late-arriving) pushes keep what the user is typing | `TicketListView.test.tsx`: "follows the URL when the search changes from outside (M-2)" + guard "keeps what the user is typing…" | ❌ input stayed "printer" → ✅ "". Guard ✅ throughout (it caught two intermediate attempts at the fix) |
| F-3 | **I-2:** the "changed by someone else — Reload" message stayed after reloading | AC-13, REQ-10 | New `hooks/useActionFailure.ts`: a version-conflict failure expires when the ticket version changes. Used by `StatusActions`, `EditTicketForm`, `AssigneeControl` | `TicketDetailsView.test.tsx`: "clears the conflict message…(I-2)", the extended edit-conflict test, guard "keeps the explanation of a rejected transition…". E2E J10 stale-message | ❌ alert present → ✅ gone. E2E J10 ✅ |
| F-4 | **M-3 / I-3:** backend URL baked in at `next build`, with a silent `localhost:8080` fallback | `architecture.md` §2, §17. `frontend/.env.example` | Runtime proxy `src/app/api/[...path]/route.ts` (reads `BACKEND_URL` per request, `500` if unset outside `next dev`, allow-listed headers, `502` if unreachable). Rewrite removed from `next.config.ts` | `src/app/api/[...path]/route.test.ts` (5). E2E harness back on non-default port 18080 | ❌ module absent / E2E `ECONNREFUSED :8080` → ✅ 5/5, E2E stub 21/21 on :18080 |
| F-5 | **Security H-1:** embedded Tomcat 11.0.24 with 3 critical advisories | Release gate (acceptance §4.5). `rules/security.md` §5 | Version catalog `tomcat = "11.0.26"`, applied as `extra["tomcat.version"]` | `EmbeddedTomcatVersionTest` (Tomcat ≥ 11.0.25) | ❌ 11.0.24 → ✅ 11.0.26. OSV: 0 advisories |

## Deliberately not changed

- **The missing backend (acceptance AC-1…12, AC-14)** isn't a defect in existing code that a small change can fix.
  It's unimplemented scope: plan STEP-08…39, blocked on the Phase 0 decisions. Treating it as a "fix" would bypass
  specification review. No test or acceptance criterion was changed to hide it. The 240 spec-acceptance failures and
  19 E2E failures remain, all with the backend's `404`.
- **Docker unavailable (AC-11):** environment, not code.
- **JUnit 6 vs TC-5, Phase 0 spec items, deployment-boundary ADR:** decisions for the engineer, not defects.

## Suite results after all fixes

| Suite | Result |
|-------|--------|
| Backend `./gradlew clean build` | ✅ 3/3 (new: `EmbeddedTomcatVersionTest`) |
| Backend `specAcceptanceTest` | ❌ 240/241 fail. **Unchanged:** the backend isn't implemented |
| Frontend `vitest` | ✅ 83/83 (was 73. +10 regression and guard tests). `lint` ✅ `typecheck` ✅ `build` ✅ |
| E2E vs contract stub (backend on :18080) | ✅ 21/21 (was 20/21) |
| E2E vs real backend (on :18080) | ❌ 19/21. Every failure is the backend's own `404` JSON (confirms the runtime proxy reaches it). J11-client and J12-down pass |
