# Server-side only, read at runtime on every /api request (spec/architecture.md §17).
# Required in production; `next dev` falls back to http://localhost:8080 when unset.
BACKEND_URL=http://localhost:8080
